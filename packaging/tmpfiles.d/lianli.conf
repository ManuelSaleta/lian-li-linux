# Shared cross-mode lock for lianli-daemon.
#
# World-writable on purpose: both the per-user daemon and the `lianli`
# system daemon (lianli user) must be able to flock it so that only
# one instance can run at a time. The file holds only a PID.
f /run/lianli-daemon.lock 0666 root root - -
